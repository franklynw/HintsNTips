import XCTest
@testable import HintsNTips

final class HintsNTipsTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(HintsNTips().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
